import { ReactNode } from "react";
export declare type ButtonProps = {
    density?: "medium";
    style?: "brand" | "danger";
    type: "button";
    emphasis: "primary";
    progress?: boolean;
    children?: ReactNode;
};
